{
  "name": "visus",
  "version": "1.0.0",
  "description": "Combines and exports the Visus JavaScript Viewer which upstream only provides as vanilla JS",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cat src/visus.js src/colormaps.js src/dvr.js src/libexport.js > src/concat.js && webpack",
    "install": "npm run build"
  },
  "main": "dist/index.js",
  "author": "Markus Martin",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
